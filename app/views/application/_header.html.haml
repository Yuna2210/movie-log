#main-navbar.collapse.navbar-collapse.custom-navmenu
  .container.py-2.py-md-5
    .row.align-items-start
      .col-md-6
        %ul.custom-menu
          %li
            -# %a{:href => "index.html"}
            =link_to 'Works', works_path
          %li
            -# %a{:href => "about.html"} About Me
            =link_to 'Directors', directors_path
          %li
            =link_to 'Contact', new_contact_path 
          -if user_signed_in?
            %li
              -if current_user.profile&.persisted?
                =link_to 'Edit your profile', edit_profile_path
              -else
                =link_to 'Create your profile', new_profile_path
            %li
              =link_to 'sign out', destroy_user_session_path
          -else
            %li
              =link_to 'sign up', new_user_registration_path
            %li
              =link_to 'sign in', new_user_session_path

      -# .col-md-6.d-none.d-md-block.mr-auto
      -#   .tweet.d-flex
      -#     %span.icofont-twitter.text-white.mt-2.mr-3
      -#     %div
      -#       %p
      -#         %em
      -#           Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam necessitatibus incidunt ut officiis explicabo inventore.
      -#           %br/
      -#           %a{:href => "#"} t.co/v82jsk
      -# -if user_signed_in?
      -#   .col-md-6.d-none.d-md-block
      -#     -if current_user.profile&.persisted?
      -#       %h3
      -#         ="#{current_user.profile.first_name} #{current_user.profile.last_name}"
      -#       %p
      -#         =image_tag current_user.profile.image_url
      -#         =current_user.profile.introducer
      -#     -else
      -#       %p
      -#         ="Welcome to our website!"
      -#         =link_to 'Create your profile', new_profile_path
      -if user_signed_in?
        .col-md-6.d-none.d-md-block
          .testimonial-wrap
            .testimonial
              -if current_user.profile&.persisted?
                -# %img.img-fluid{:alt => "Image", :src => "assets/img/person_1.jpg"}/
                =image_tag current_user.profile.image_url, class: 'img-fluid'
                %blockquote
                  %p=current_user.profile.introducer
                %p="#{current_user.profile.first_name} #{current_user.profile.last_name}"
              -else
                %p
                  ="Welcome to our website!"
                  =link_to 'Create your profile', new_profile_path


%nav.navbar.navbar-light.custom-navbar
  .container
    -# %a.navbar-brand{:href => "index.html"} MyPortfolio.
    %p=link_to 'Movie log', works_path, class: 'navbar-brand'
    %a.burger{"data-target" => "#main-navbar", "data-toggle" => "collapse", :href => "#"}
      %span
